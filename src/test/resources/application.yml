spring:
  cloud:
    gcp:
      sql:
        enabled: false
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:YouthCouncilDev
    username: "sa"
    password: ""
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  profiles:
    active: test # prod, test
  messages:
    basename: templates/details
server:
  port: 8080
