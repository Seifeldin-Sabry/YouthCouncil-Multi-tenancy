spring:
  cloud:
    gcp:
      sql:
        enabled: false
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  profiles:
    active: dev # prod, test
  messages:
    basename: templates/details
server:
  port: 8080
